# Render Blueprint - use repo root for build/start
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: kd-sarees
    runtime: node
    # Root Directory must be empty so root package.json is used
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
